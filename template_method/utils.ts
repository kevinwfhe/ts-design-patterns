/* c8 ignore start */

// Util functions are mocked in tests
export const cloneCmd = async () => {
  return Promise.resolve(true);
};

export const installCmd = async () => {
  return Promise.resolve(true);
};

export const testCmd = async () => {
  return Promise.resolve(100);
};

export const buildCmd = async () => {
  return Promise.resolve(true);
};

export const publishCmd = async () => {
  return Promise.resolve(true);
};
/* c8 ignore stop */